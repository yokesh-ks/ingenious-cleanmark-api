{
	"name": "url-to-markdown",
	"version": "1.0.0",
	"main": "src/index.js",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "biome format --write"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "Convert URLs to Markdown using Cloudflare Workers",
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"wrangler": "^4.59.3"
	},
	"dependencies": {
		"@mozilla/readability": "^0.5.0",
		"html-entities": "^2.5.2",
		"linkedom": "^0.18.9",
		"turndown": "^7.2.0"
	}
}
