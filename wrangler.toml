name = "url-to-markdown-convertor"
main = "src/index.js"
compatibility_date = "2024-12-30"
compatibility_flags = ["nodejs_compat"]

# Run Worker in datacenter closest to target website (not user)
# This helps bypass geo-blocking since StackOverflow is US-based
[placement]
mode = "smart"

[observability]
enabled = true
